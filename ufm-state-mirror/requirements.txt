# Runtime dependencies for the ufm-state-mirror sidecar.
# Pinned for reproducible image builds.
redis==5.0.8
PyYAML==6.0.2
watchdog==4.0.2
# ConfigMap storage backend (HLD 5.3.x): talks to the apiserver via CoreV1Api.
kubernetes==31.0.0
