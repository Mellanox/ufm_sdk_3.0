<source>
#   @type forward # for sending the data without compression
  @type http
  bind 0.0.0.0
  port 24225 
</source>
<match UFM_Telemetry_Streaming**>
  @type stdout
#  @type file
#  path /tmp/fluentd-stremaing2/data.log
</match>