{{- $ufmFullname := .Values.ufmFullname }}
{{- $namespace := include "ufm-plugin.namespace" . }}
{{- $pvcClaimName := include "ufm-plugin.pvcClaimName" . }}
{{- range .Values.plugins.items }}
{{- if and .name .image .tag }}
{{- $k8sName := .name | replace "_" "-" }}
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ $ufmFullname }}-plugin-{{ $k8sName }}
  namespace: {{ $namespace }}
spec:
  replicas: 1
  selector:
    matchLabels:
      app: {{ $ufmFullname }}-plugin-{{ $k8sName }}
  template:
    metadata:
      labels:
        app: {{ $ufmFullname }}-plugin-{{ $k8sName }}
        plugin-name: {{ $k8sName }}
    spec:
      {{- with $.Values.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with $.Values.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with $.Values.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with $.Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      initContainers:
        - name: {{ $k8sName }}-init
          image: "{{ .image }}:{{ .tag }}"
          imagePullPolicy: {{ .imagePullPolicy | default "IfNotPresent" }}
          command: ["/bin/sh", "-c"]
          args:
            - /init.sh -ufm_version ${UFM_VERSION}
          env:
            - name: UFM_VERSION
              valueFrom:
                configMapKeyRef:
                  name: {{ $ufmFullname }}-config
                  key: UFM_VERSION
          securityContext:
            privileged: false
            {{- if and $.Values.rdma.resourceName (ne (toString $.Values.rdma.resourceCount) "0") }}
            capabilities:
              add:
                - IPC_LOCK
            {{- end }}
          resources:
            {{- if and $.Values.rdma.resourceName (ne (toString $.Values.rdma.resourceCount) "0") }}
            limits:
              {{ $.Values.rdma.resourceName }}: {{ $.Values.rdma.resourceCount }}
            {{- end }}
          volumeMounts:
            - name: ufm-files
              mountPath: /config
              subPath: conf/plugins/{{ .name }}
            - name: ufm-files
              mountPath: /log
              subPath: log/plugins/{{ .name }}
      containers:
        - name: {{ $k8sName }}
          image: "{{ .image }}:{{ .tag }}"
          imagePullPolicy: {{ .imagePullPolicy | default "IfNotPresent" }}
          securityContext:
            privileged: false
            {{- if or (and $.Values.rdma.resourceName (ne (toString $.Values.rdma.resourceCount) "0")) .extraCapabilities }}
            capabilities:
              add:
                {{- if and $.Values.rdma.resourceName (ne (toString $.Values.rdma.resourceCount) "0") }}
                - IPC_LOCK
                {{- end }}
                {{- with .extraCapabilities }}
                {{- toYaml . | nindent 16 }}
                {{- end }}
            {{- end }}
          env:
{{- if .port }}
            - name: PLUGIN_PORT
              value: "{{ .port }}"
{{- end }}
{{- if .healthEndpoint }}
            - name: HEALTH_ENDPOINT
              value: "{{ .healthEndpoint }}"
{{- if or .healthPort .port }}
            - name: HEALTH_PORT
              value: "{{ .healthPort | default .port }}"
{{- end }}
{{- end }}
{{- with .env }}
            {{- toYaml . | nindent 12 }}
{{- end }}
          volumeMounts:
            - name: ufm-files
              mountPath: /config
              subPath: conf/plugins/{{ .name }}
            - name: ufm-files
              mountPath: /log
              subPath: log/plugins/{{ .name }}
            - name: health-scripts
              mountPath: /health-scripts
              readOnly: true
            {{- with .volumeMounts }}
            {{- toYaml . | nindent 12 }}
            {{- end }}
          resources:
            {{- $res := .resources | default $.Values.plugins.defaultResources }}
            {{- if $res.requests }}
            requests:
              {{- toYaml $res.requests | nindent 14 }}
            {{- end }}
            {{- if or $res.limits (and $.Values.rdma.resourceName (ne (toString $.Values.rdma.resourceCount) "0")) }}
            limits:
              {{- if $res.limits }}
              {{- toYaml $res.limits | nindent 14 }}
              {{- end }}
              {{- if and $.Values.rdma.resourceName (ne (toString $.Values.rdma.resourceCount) "0") }}
              {{ $.Values.rdma.resourceName }}: {{ $.Values.rdma.resourceCount }}
              {{- end }}
            {{- end }}
          {{- if .disableLivenessProbe }}
          {{- else }}
          {{- if .livenessProbe }}
          livenessProbe:
            {{- toYaml .livenessProbe | nindent 12 }}
          {{- else }}
          {{- if .healthEndpoint }}
          # Default liveness: native httpGet (set healthEndpoint and healthPort/port)
          livenessProbe:
            httpGet:
              path: {{ .healthEndpoint }}
              port: {{ .healthPort | default .port }}
            initialDelaySeconds: 60
            periodSeconds: 30
            timeoutSeconds: 15
            failureThreshold: 3
          {{- else }}
          {{- if .port }}
          # Default liveness: native tcpSocket (set port)
          livenessProbe:
            tcpSocket:
              port: {{ .port }}
            initialDelaySeconds: 60
            periodSeconds: 30
            timeoutSeconds: 15
            failureThreshold: 3
          {{- end }}
          {{- end }}
          {{- end }}
          {{- end }}
          {{- with .readinessProbe }}
          readinessProbe:
            {{- toYaml . | nindent 12 }}
          {{- end }}
      volumes:
        - name: ufm-files
          persistentVolumeClaim:
            claimName: {{ $pvcClaimName }}
        - name: health-scripts
          configMap:
            name: {{ $ufmFullname }}-plugin-health-scripts
            defaultMode: 0755
        {{- with .volumes }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
{{- end }}
{{- end }}
