# CI: validate per-plugin strategy override (default is Recreate, this plugin uses RollingUpdate)
ufmFullname: "ufm-ufm-enterprise"
namespace: "ufm-enterprise"

plugins:
  entries:
    rolling_plugin:
      image: busybox
      tag: "1.36"
      port: 8080
      strategy: RollingUpdate
