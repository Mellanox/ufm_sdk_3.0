# CI: validate chart with watchdog disabled (no watchdog labels, annotations, or nodeAffinity)
ufmFullname: "ufm-ufm-enterprise"
namespace: "ufm-enterprise"

watchdog:
  enabled: false

plugins:
  items:
    - name: ci_no_watchdog
      image: busybox
      tag: "1.36"
      port: 8080
